__pycache__/
*.pyc
.DS_Store

# Private member matchup files pass through here and are deleted after
# reporting. Never commit, never push.
inbox/

# Belt and braces: member matchup files must never be committed from anywhere
# in the tree, not just inbox/.
*matchup*.csv
*_matchup*
